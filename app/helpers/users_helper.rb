module UsersHelper   

    # def 

    # def authenticate_user
    #     if User.current
    #         @user = User.find(User.current[:id])
    #     end
        
    #     unless User.current.logged?
    #         redirect_to root_path, :flash => { :notice => "You must be logged in to access this section." }
    #     end
    # end
end
