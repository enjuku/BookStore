%h1 Editing user

= simple_form_for @user,
                :url => { :action => "account" },
                :html => { :method => :post, :multipart => true } do |f|
    = f.input :name, placeholder: "Name", label: false, required: true
    = f.input :email, label: false, disabled: true, hint: 'You cannot change your email.'
    = f.button :submit

%hr
= link_to 'Change password', lost_password_path

%hr
= link_to "Delete account", destroy_path, data: { confirm: "Your account will be destroyed, this action cannot be undone. Proceed?" }

- if User.current.admin?
    = link_to 'Show', @user
    \|
    = link_to 'Show all users', users_path
