%h1 Forgot your password?

%p Enter a new password for your #{@user.email} account.

= simple_form_for @user, url: password_recovery_path(params[:id]) do |f|
    = f.input :password, placeholder: "Password", label: false, required: true, autofocus: true
    = f.input :password_confirmation, placeholder: "Password confirmation", label: false, required: true
    = hidden_field_tag :email, @user.email
    = f.button :submit, "Reset password"