-# - if @genres.present?
-#     - @genres.each do |genre|
-#         %p= "Genre: #{genre.name}"

%h1 Listing genres

%table
    %thead
        %tr
        %th Genre
        %th
        %th

    %tbody
    - @genres.each do |genre|
        %tr
            %td= genre.name
            %td= link_to "Edit", edit_genre_path(genre)
            %td= link_to "Delete", genre_path(genre), data: { confirm: "Are you sure?" }, :method => :delete

%br

= link_to 'New genre', new_genre_path